# DECISIONS
Append-only. Superseding a decision means adding a new record that names the one it replaces — never editing in place.

| ID | Decision | Authority | Date | Evidence |
|---|---|---|---|---|
| **D-001** | Top-50 ranked on **IMF WEO `NGDPD`**, not World Bank `NY.GDP.MKTP.CD`. WB cross-check computed in parallel, never spliced. | Claude (economic definition, §38) | 2026-08-17 | WB has **no Taiwan** among 217 economies; WB-based Top-50 omits the ~#22 economy and promotes Iraq to #50. WB fiscal coverage 15/50 vs WEO 50/50 → denominator consistency also requires it. |
| **D-002** | Reference year = latest year with **non-projected** data for ≥95% of ranked economies, derived from the WEO bulk *Estimates Start After* field. Never "latest with any data." | Claude | 2026-08-17 | At probe: WB 2025 missing UAE (GDP), **US + Argentina** (CPI), **US + Japan** (exports), **US + China + Japan** (investment). |
| **D-003** | Universe = *reporting economies*. Include TWN and HKG; exclude all aggregates by reference-list filter; carry `monetary_union` (10 euro-area members among the 50). | Claude | 2026-08-17 | WB `/country` = 295 entities, **78 aggregates**. IMF mixes `WEOWORLD`/`ADVEC`/`EURO` into the same values dict. |
| **D-004** | One metric, one primary source, whole history. Cross-checks are parallel series with divergence tolerances. **Never average sources; never splice to extend coverage.** | Claude | 2026-08-17 | — |
| **D-005** | Ingest the full ~200-economy panel; Top-50 is a dated materialised view. Historical rank views read the panel, not the current list. | Claude + Codex (architecture) | 2026-08-17 | Required for rank-history and to avoid survivorship bias (G-9). |
| **D-006** | Observation store is **append-only and vintage-keyed** `(metric_id, iso3, year, source_vintage)`, with `observation_class` NOT NULL. | Claude (requirement) / Codex (implementation) | 2026-08-17 | WDI vintage `2026-07-13`; WEO rewrites history twice yearly; ICP 2021 retrospectively revised PPP levels. Cannot be retrofitted. |
| **D-007** | Comparability class (A/B/C) and `population_scope` **govern UI capability**, not just tooltips. Class C blocked from ranking axes; `subset:*` structurally barred. | Claude | 2026-08-17 | This is the product thesis expressed as code. |
| **D-008** | **Source substitutions:** debt → WEO `GGXWDG_NGDP`; fiscal balance → WEO `GGXCNL_NGDP`; CPI → WEO `PCPIPCH`; unemployment → WB `SL.UEM.TOTL.ZS` (ILO-modelled). | Claude | 2026-08-17 | Measured coverage: 50/50 vs 15/50, 50/50 vs 40/50, 50/50 vs 48/50 (WB missing **US**), 50/50 vs 48/50. |
| **D-009** | **Do not ingest in Phase 1:** `DT.DOD.DECT.CD`, `FR.INR.RINR`, `FR.INR.LEND`, `GC.DOD.TOTL.GD.ZS`, `SL.UEM.TOTL.NE.ZS`. | Claude | 2026-08-17 | 19/50 (and subset-by-construction), 15/50, 15/50, 15/50, and definitionally incompatible with M-014. |
| **D-010** | Gini and top-decile share are **Class C**, country-page only, and **barred from every composite score**. The inequality dimension is dropped from the Economic Performance Score rather than imputed. | Claude | 2026-08-17 | Clears 40/50 in **2018 only**; 9 of the 50 absent even then; surveys mix consumption/income and gross/net. See Q-008. |
| **D-011** | Composite scores publish a **rank interval** (5th–95th percentile over 2,000 Dirichlet-sampled weight vectors), not a point rank. Overlapping intervals are reported as *not distinguishable*. | Claude | 2026-08-17 | Exposing sliders is insufficient — users anchor on the default. Supersedes the master prompt §9 single-score presentation. |
| **D-012** | Forecast benchmark is the **IMF WEO forecast at matching horizon on true out-of-sample vintages**, not a naive baseline. Requires vintage archiving from Phase 1. | Claude | 2026-08-17 | We already ingest WEO forecasts to 2031; beating a random walk is not a claim worth making. See Q-009. |
| **D-013** | Master prompt §19 "Growth Drivers" is renamed **"Historical Associations"** in all code, config, and UI copy. Specifications are pre-registered in-repo before running; all are displayed, not the best one. | Claude | 2026-08-17 | Nickell bias at N=50/T≈30 with LDV; investment→growth is the canonical endogeneity case; tree ensembles memorise country identity. |
